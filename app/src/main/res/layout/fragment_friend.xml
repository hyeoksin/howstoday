<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".fragment.bottom.talks.FriendFragment">

    <LinearLayout
        android:id="@+id/talks_friend_search_area"
        android:visibility="gone"
        android:orientation="horizontal"
        android:padding="5dp"
        android:layout_width="match_parent"
        android:layout_height="45dp">

        <EditText
            android:id="@+id/talks_friend_searchedit"
            android:inputType="textMultiLine"
            android:hint="검색할 친구의 이메일을 입력하세요."
            android:textSize="16dp"
            android:background="#fff"
            android:maxLines="1"
            android:padding="5dp"
            android:layout_weight="7"
            android:layout_width="0dp"
            android:layout_height="match_parent"/>

        <Button
            android:id="@+id/talks_friend_searchbtn"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:layout_gravity="center"
            android:textSize="16dp"
            android:text="찾기"
            android:background="#fff"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"/>

    </LinearLayout>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/talks_friends_recyclerview"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        </androidx.recyclerview.widget.RecyclerView>
    </LinearLayout>

</LinearLayout>